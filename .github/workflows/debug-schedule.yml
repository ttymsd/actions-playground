name: Debug schedule

on:
  schedule:
    - cron: '*/5 * * * *' # 平日毎朝7時に実行 (UTC/協定世界時)

jobs:
  build:
    name: KtLint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - name: Test
        run: echo $GITHUB_SHA
